# `/sen:split`

Create leakage-safe splits and test for leakage.

Keep split config in `config/split/split.yaml` and split code in `src/data/001_split.py` or the next numbered file. Check for target leakage, temporal leakage, entity overlap, train/test contamination, and preprocessing fitted before the split.
