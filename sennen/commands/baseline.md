# `/sen:baseline`

Establish a reproducible baseline to beat.

Create baseline code in `src/experiment/001_baseline.py` or the next numbered file. Keep assumptions simple, align with the chosen metrics, and version large outputs with DVC when available. Use MLflow for baseline metrics when the repo already uses it or the user explicitly asks for tracked runs.
